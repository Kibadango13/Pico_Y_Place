import styled from "styled-components";

import { TrafficRegulationPredictorStyledProps as Props } from "./TrafficRegulationPredictor.types";

const TrafficRegulationPredictorStyled = styled.div<Props>`
  .TrafficRegulationPredictor {
  }
`;

export default TrafficRegulationPredictorStyled;

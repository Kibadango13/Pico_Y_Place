// Interfaces and types from component TrafficRegulationPredictor

// Component Props
export interface TrafficRegulationPredictorProps {}

export interface FormConfig {
  licensePlate: string;
  selectedDate: string;
  selectedHour: string;
}

// Styled Component Props
export interface TrafficRegulationPredictorStyledProps {
  className: string;
}
